import { Head } from '@inertiajs/react';
import AppLayout from '@/layouts/app-layout';

export default function ProjectTypeForm() {
    return (
        <AppLayout>
            <Head title="Project Type Form" />
            <div>
                <h1>Project Type Form</h1>
                <p>This page is not used directly. The form is handled in a modal on the index page.</p>
            </div>
        </AppLayout>
    );
}
